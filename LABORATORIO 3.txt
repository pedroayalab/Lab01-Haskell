1) x = [1,2,3]
y = [4,5,6,7,8,9]
z=x++y
z
2) :t True
 :t "Hola mundo"
 :t [1,2,3]
3)
que sea fuertemente tipado significa que cuando le das un tipo a una variable no se puede cambiar es decir,
si una varible se define como tipo String no se puede convertir en entero
es una técnica de programación que retrasa el cálculo de expresiones hasta que su resultado es estrictamente necesario. Evita cálculos repetidos o innecesarios, lo que mejora el rendimiento, permite manejar estructuras de datos infinitas y optimiza el uso de memoria
4) 
double x = x*2
double 2
esMayorDeEdad edad = edad > 18
esMayorDeEdad 16
esMayorDeEdad 30
5)
areaCirculo radio = pi *radio^2
:t areaCirculo 2
6)
head [10,20,30,40]
tail [10,20,30,40]
take 3 [1..10]
drop 4 [1..10]
length [1,2,3,4,5]
la expression [1...10] hace una lista de de 1 a 10  en Haskell se relaciona con la evaluación perezosa porque no se genera ni almacena en memoria completa inmediatamente. Haskell solo calcula los elementos necesarios y bajo demanda, permitiendo definir rangos grandes o infinitos (como [1..]) sin colapsar el sistema, evaluando solo lo requerido por funciones como take o head
7)
nElem :: [Int] -> Int -> Int
nElem [] n = -1
nElem (x:xs) n = if n == 0 then x else nElem xs (n-1)
nElem  [1,2,3] 2
8) productoLista :: [Int] -> Int
productoLista(x:xs) = if length xs == 0 then x else x * productoLista xs
productoLista [1,2,3]
9) 
eliminarNegativos :: [Int] -> [Int]
eliminarNegativos [] = []
eliminarNegativos (x:xs) = if x >= 0 then [x] ++ eliminarNegativos xs else  eliminarNegativos xs
eliminarNegativos [2,-1]
10)
invertir :: [a] -> [a]
invertir [] = []
invertir (x:xs) = invertir xs ++ [x]
invertir [1,2,3]

11)
suma x y = x+y
cuadrado x = x*x
sumaCuadrados :: Int -> Int -> Int
sumaCuadrados x y = suma (cuadrado x) (cuadrado y)
sumaCuadrados 2 
12)
double x = x*2
double 2
dobleLista :: [Int] -> [Int]
dobleLista x = map (double) x
dobleLista [1,2,3]
13)
clasificarNota :: Int -> String
clasificarNota x = if x <= 3 then "reprobado" else if x> 3 && x < 4 then "en recuperacion" else "aprobado"
clasificarNota 4

14) 
fibonacci :: Int -> Int
fibonacci 0 = 0
fibonacci 1 = 1
fibonacci x =  (fibonacci x-1) + (fibonacci x-2)
fibonacci 2
